import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'todo-architecture-selector',
  imports: [RouterLink],
  templateUrl: './architecture-selector.html',
  styleUrl: './architecture-selector.scss',
})
export class ArchitectureSelector {}
